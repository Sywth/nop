factorial <- (x) {
    return if (
        <(x,2), 
        (){
            return 1
        }, 
        (){
            return *(
                x,
                self(-(x,1))
            )
        }
    )
}


factorial <- (x) {return if (<(x,2), (){return 1}, (){return *(x,self(-(x,1)))})}
